import { Button } from "@heroui/react";

const RegisterPatientButton = (params) => {
    return (
        <div>
            <Button size="md" radius="none">Registrar Peciente</Button>
        </div>
    );
}

export default RegisterPatientButton;