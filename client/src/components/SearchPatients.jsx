import { Input } from "@heroui/react";

const SearchPatients = (params) => {
    return (
        <div>
            <Input label="Buscar pacientes" type="text"  size="sm"/>
        </div>
    );
}

export default SearchPatients;